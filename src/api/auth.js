// src/api/auth.js
// Funções relacionadas à autenticação (login, logout, etc.)

export const login = async (credentials) => {
    // Implemente a lógica de login aqui
};

export const logout = async () => {
    // Implemente a lógica de logout aqui
};
